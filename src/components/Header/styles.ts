import styled from 'styled-components'
import { defaultTheme } from '../../styles/themes/default'

export const HeaderContainer = styled.header`
  grid-area: header;
  padding: 10px;
  background-color: ${defaultTheme['purple-900']};
`
