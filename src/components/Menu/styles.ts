import styled from 'styled-components'
import { defaultTheme } from '../../styles/themes/default'

export const MenuContainer = styled.nav`
  grid-area: menu;
  background-color: ${defaultTheme['purple-600']};
`
